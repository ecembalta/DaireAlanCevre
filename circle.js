module.exports = {
  circleArea,
  circleCircumference,
};

function circleArea(r) {
  let area = Math.PI * r * r;
  console.log('Dairenin Alanı:', area);
}

function circleCircumference(r) {
  let circumference = Math.PI * 2 * 2;
  console.log('Dairenin Çevresi:', circumference);
}
